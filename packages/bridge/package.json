{
  "name": "pi-sandbox-dsh-bridge",
  "version": "0.1.0",
  "type": "module",
  "description": "Shared contract for pi-sandbox-dsh: continuous-agent global sandbox tier ladder (read-only / workspace-write / danger-full-access), strict-widening escalation vocabulary, per-call policy resolution, denial/hint markers, fail-closed vocabulary. Single reference source: dsh.",
  "keywords": [
    "pi-package",
    "pi-sandbox"
  ],
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types test/bridge.spec.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.86.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
